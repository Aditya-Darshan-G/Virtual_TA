id Remove all stopped containers podman container prune
``` You can create a Dockerfile to build a container image. Here's a sample Dockerfile that converts a Python script into a container image. ```dockerfile
FROM python:3.11-slim Set working directory WORKDIR /app Typically, you would use COPY . . to copy files from the host machine, but here we're just using a simple script. RUN echo 'print("Hello, world!")' > app.py Run the script CMD ["python", "app.py"]
``` To build, run, and deploy the container, run these commands: ```bash Create an account on https://hub.docker.com/ and then login podman login docker.io Build and run the container podman build -t py-hello .
podman run -it py-hello Push the container to Docker Hub. Replace $DOCKER_HUB_USERNAME with your Docker Hub username. podman push py-hello:latest docker.io/$DOCKER_HUB_USERNAME/py-hello Push adding a specific tag, e.g.

---

